<% unless current_user?(@user) %>
  <div id="follow_platter_form">
    <% if current_user.following_platter?(@platter) %>
      <%= render 'unfollow_platter' %>
    <% else %>
        <%= render 'follow_platter' %>
    <% end %>
  </div>
<% end %>